[project]
name = "telegram-ops-alerts"
version = "1.0.0"
description = "Deduplicate JSON operational events and optionally notify Telegram"
license = { text = "MIT" }
requires-python = ">=3.11"

[project.urls]
Repository = "https://github.com/Utasu/telegram-ops-alerts"

[tool.ruff]
line-length = 100
